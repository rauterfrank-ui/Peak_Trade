name: CI / Operator Verify (Registry)

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  operator-verify:
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5

      - name: Setup Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.11"

      - name: Install tooling
        run: |
          python -V
          pip -V
          pip install -U pip
          pip install ruff
          sudo apt-get update -qq && sudo apt-get install -y ripgrep

      - name: Ruff check (fast)
        run: |
          ruff check .

      - name: gh auth (token)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh auth status

      - name: Operator verify (download + invariants)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          scripts/ops/verify_from_registry.sh docs/ops/registry/LATEST_PHASE_M_SMOKE.pointer --download
