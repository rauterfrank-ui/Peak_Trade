name: CI

on:
  push:
    branches: ["main", "master"]
  pull_request:
    branches: ["main", "master"]

jobs:
  tests:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        python-version: ["3.9", "3.10", "3.11"]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt', '**/pyproject.toml') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-cov

      - name: Run tests
        run: |
          pytest tests/ -v --tb=short

      - name: Run tests with coverage
        if: matrix.python-version == '3.11'
        run: |
          pytest tests/ --cov=src --cov-report=xml --cov-report=term-missing

      # TODO: Linting (optional, aktivieren wenn gewünscht)
      # - name: Run ruff
      #   run: |
      #     pip install ruff
      #     ruff check src/ tests/

  # ============================================================================
  # CI Smoke Fast-Lane (Issue #19 / G3-1)
  # ============================================================================
  # Ultra-fast smoke tests (<2-3 min) covering live pipeline entrypoints.
  # Runs BEFORE main test matrix for rapid feedback on critical paths.
  #
  # Safety gates tested:
  # - Live risk limits (position size, exposure) - Issue #20
  # - Execution pipeline (signal → order lifecycle)
  # - Core backtest engine (baseline functionality)
  #
  # GUARANTEES:
  # - NO live/testnet exchanges
  # - NO API keys or secrets
  # - NO external network calls
  # - Fully deterministic (synthetic data only)
  # ============================================================================
  ci-smoke-fastlane:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-smoke-${{ hashFiles('**/requirements.txt', '**/pyproject.toml') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest

      - name: Run CI smoke fast-lane
        run: |
          make ci-smoke

      - name: Upload smoke test artifacts on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: ci-smoke-fastlane-debug
          path: test_results/ci_smoke_fastlane/
          retention-days: 7

  # ============================================================================
  # Strategy Smoke Tests (Phase 77)
  # ============================================================================
  # Runs the strategy smoke-check CLI and associated tests to ensure all
  # v1.1 strategies pass basic validation. This is a safety gate - if any
  # strategy fails the smoke test, the CI build fails.
  #
  # IMPORTANT: This job runs OFFLINE only - no live/testnet exchanges,
  # no API keys, no secrets. Uses only synthetic/backtest data.
  # ============================================================================
  strategy-smoke:
    runs-on: ubuntu-latest
    needs: tests  # Run after main tests pass

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt', '**/pyproject.toml') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest

      - name: Run strategy smoke pytest
        run: |
          pytest tests/test_strategy_smoke_cli.py -v --tb=short

      - name: Create smoke results directory
        run: |
          mkdir -p test_results/strategy_smoke

      - name: Run strategy smoke CLI
        run: |
          python scripts/strategy_smoke_check.py \
            --output-json test_results/strategy_smoke/ci_smoke_latest.json \
            --output-md test_results/strategy_smoke/ci_smoke_latest.md

      - name: Upload smoke test artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: strategy-smoke-results
          path: test_results/strategy_smoke/
          retention-days: 30
