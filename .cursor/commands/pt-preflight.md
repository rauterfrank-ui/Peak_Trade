# /pt-preflight

Run a robust repo preflight. If stuck in a shell continuation prompt (>, dquote>, heredoc>), press Ctrl-C first.

Then:
1) Print pwd and git root
2) Print git status summary
3) Print current branch and upstream info

Output:
- A short "OK/NOT OK" summary and what to fix if not OK.
