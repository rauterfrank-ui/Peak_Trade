# =============================================================================
# Peak_Trade MCP Environment (LOCAL ONLY)
# =============================================================================
# Copy this file to: .cursor/.env
#
# IMPORTANT:
# - `.env` files are ignored by git in this repo.
# - Cursor does NOT automatically load `.env` files.
# - You must export these variables in the environment that starts Cursor
#   (e.g. start Cursor from a terminal where you `source .cursor/.env`).
# =============================================================================

# Grafana base URL (local Grafana example)
GRAFANA_URL=http://localhost:3000

# Service account token (Grafana)
# Create via: Grafana UI -> Administration -> Service Accounts -> Add token
GRAFANA_SERVICE_ACCOUNT_TOKEN=REPLACE_WITH_REAL_TOKEN
