name: Bad Path Filtered Required

on:
  pull_request:
    paths:
      - ".github/workflows/**"
  push:
    branches: [main]

jobs:
  path-filtered-check:
    name: path-filtered-check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run check
        run: echo "This check is path-filtered"
