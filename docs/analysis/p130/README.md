# P130 — Networked Allowlist Stub v1 (default-deny)

Scope: shadow/paper only. Networkless by default.

Adds an additional hard gate for networked execution: **adapter + market allowlist**.
This is *in addition* to the existing Transport Gate (P125/P128), which remains default-deny.
