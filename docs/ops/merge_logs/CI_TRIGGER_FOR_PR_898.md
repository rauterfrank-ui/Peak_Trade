# CI trigger for PR 898

This file exists to ensure required status checks run on a PR that primarily changes workflow configuration.

It can be removed later once the merge-logs-sanity workflow is on main and the system is stable.
