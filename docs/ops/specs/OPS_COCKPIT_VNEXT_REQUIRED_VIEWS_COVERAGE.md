# OPS Cockpit — vNext Required Views Coverage (Traceability)

**status:** active  
**last_updated:** 2026-04-13  
**purpose:** Review-friendly **mapping** from [`OPS_SUITE_DASHBOARD_VNEXT_SPEC.md`](OPS_SUITE_DASHBOARD_VNEXT_SPEC.md) **Required Views §1–7** to Ops Cockpit **payload keys**, **operator summary / HTML surface** (see [`OPS_COCKPIT_OPERATOR_SUMMARY_SURFACE.md`](OPS_COCKPIT_OPERATOR_SUMMARY_SURFACE.md)), and **test anchors**; plus a **supplementary** subsection for additional Cockpit surfaces that are **repo-present** but **outside** the vNext Required-Views matrix (§1–7). **Not** a completeness guarantee for every bullet under each view; **not** an execution or approval claim.

**docs_token:** `DOCS_TOKEN_OPS_COCKPIT_VNEXT_REQUIRED_VIEWS_COVERAGE`

## Non-Goals

- **Not** broker, exchange, or reconciliation truth — same semantics as [`OPS_COCKPIT_PAYLOAD_READ_MODEL_CONTRACT.md`](OPS_COCKPIT_PAYLOAD_READ_MODEL_CONTRACT.md) and [`RUNBOOK_OPS_SUITE_PHASE_E_GOVERNANCE_REVIEW.md`](../runbooks/RUNBOOK_OPS_SUITE_PHASE_E_GOVERNANCE_REVIEW.md).  
- **Not** a substitute for [`OPS_COCKPIT_OPERATOR_SUMMARY_SURFACE.md`](OPS_COCKPIT_OPERATOR_SUMMARY_SURFACE.md) (detailed row-level mapping).  
- **Not** coverage of **R&amp;D Dashboard** (Phase 76) or non-Cockpit UIs.

## Canonical anchors

| Anchor | Role |
|--------|------|
| [`OPS_SUITE_DASHBOARD_VNEXT_SPEC.md`](OPS_SUITE_DASHBOARD_VNEXT_SPEC.md) | Product vNext Required Views §1–7 (target). |
| [`OPS_COCKPIT_OPERATOR_SUMMARY_SURFACE.md`](OPS_COCKPIT_OPERATOR_SUMMARY_SURFACE.md) | Payload ↔ operator-summary HTML (primary detail). |
| [`OPS_COCKPIT_PAYLOAD_READ_MODEL_CONTRACT.md`](OPS_COCKPIT_PAYLOAD_READ_MODEL_CONTRACT.md) | Top-level payload keys for `build_ops_cockpit_payload`. |
| `tests&#47;webui&#47;test_ops_cockpit.py` | Cockpit HTML &#47; payload builder regression (representative). |
| `tests&#47;ops&#47;test_ops_cockpit_payload_top_level_contract.py` | Stable top-level key set (no value snapshots). |
| `tests&#47;ops&#47;test_*_observation.py` | Per-aggregate tests for compact `*_observation` builders (where present). |

## Required Views §1–7 ↔ Cockpit read-model (summary)

| RV | vNext Required View (headline) | Primary payload / observation keys (indicative) | Operator summary / HTML (indicative) | Test / doc anchors |
|----|----------------------------------|-------------------------------------------------|--------------------------------------|---------------------|
| **1** | System State | `system_state`, `system_state_observation` | `_render_operator_summary_surface` — **System status**, **System &#47; environment (observation)**; hero **Config environment (observation)** | `test_system_state_observation.py`; operator summary surface table |
| **2** | Go &#47; No-Go | `policy_state`, `guard_state`, `operator_state`, `policy_go_no_go_observation` | **Go &#47; No-Go observation**, **Policy &#47; go-no-go (observation)** | `test_policy_go_no_go_observation.py`; `test_ops_cockpit_payload_top_level_contract.py` |
| **3** | Session &#47; Run State | `run_state`, `session_end_mismatch_state`, `stale_state` (in part), `run_session_observation`, `operator_state` (inputs) | **Run &#47; session (observation)**; cards **Run state**, **Stale State**; **Session End Mismatch** | `test_run_session_observation.py`; `tests&#47;webui&#47;test_ops_cockpit.py` (session &#47; run cases) |
| **4** | Incident &#47; Safety | `incident_state`, `dependencies_state`, `incident_safety_observation` | **Incident &#47; safety (observation)**; **Incident observation (read-only)** | `test_incident_safety_observation.py` |
| **5** | Exposure &#47; Risk | `exposure_state`, `transfer_ambiguity_state`, `stale_state`, `balance_semantics_state`, `exposure_risk_observation` | **Exposure &#47; risk (observation)**; **Exposure State**, **Transfer &#47; Treasury ambiguity**, **Stale State** | `test_exposure_risk_observation.py`; exposure contract refs in operator summary surface |
| **6** | Policy &#47; Governance | `policy_state`, `guard_state`, `ai_boundary_state`, `human_supervision_state`, `evidence_state` (cross-ref), `governance_boundary_observation` | `_render_policy_governance_observation_surface` — **Policy &#47; Governance observation (vNext RV6)** | `test_governance_boundary_observation.py` |
| **7** | Health &#47; Drift | `health_drift_observation`, `truth_status`, `freshness_status`, `source_coverage_status`, `executive_summary`, `evidence_state`, `dependencies_state`, `stale_state` (incl. `order`; see Note — `order_staleness_reader`) | **Health &#47; drift (observation)**; **Status-at-a-glance**; **Stale State** card (incl. `stale_state.order`); **Evidence** blocks as linked in summary surface | `test_health_drift_observation.py`; `test_evidence_audit_observation.py`; `tests&#47;webui&#47;test_ops_cockpit.py` — `test_stale_state_order_*` |

**Notes:** `safety_posture_observation` spans gating posture (related to RV2 &#47; RV4); see operator summary surface ordering. **`stale_state.order`:** when non-empty `live_runs&#47;*&#47;events.*` exist, `stale_state.order` is set read-only in `build_ops_cockpit_payload` via `get_live_runs_order_staleness` in `src/live/order_staleness_reader.py` (event log **mtime** vs age threshold; values **`ok`** / **`stale`** / **`unknown`**). This is **not** exchange order-book state — same semantics as the **Note — `stale_state.order`** in [`OPS_COCKPIT_OPERATOR_SUMMARY_SURFACE.md`](OPS_COCKPIT_OPERATOR_SUMMARY_SURFACE.md). **Additional** Cockpit keys not named in vNext §1–7 but shipped read-only are trace-mapped in **Supplementary Cockpit surfaces** below; they **do not** replace Required View semantics.

## Supplementary Cockpit surfaces (beyond Required Views §1–7)

These surfaces are **in the Ops Cockpit read model** and **documented** here for the same **traceability style** as the RV1–7 table — **not** as new Required Views, **not** as product scope expansion, and **without** claiming every HTML block or nested field is listed.

| Surface | Zweck (kurz) | Relevante Payload-Anker | Operator summary &#47; HTML &#47; Page | Tests &#47; Doc-Anker | Non-Goals &#47; Non-Claims |
|---------|----------------|-------------------------|----------------------------------------|----------------------|----------------------------|
| **`phase83_eligibility_snapshot`** | Read-only **Snapshot** der bestehenden Phase-83-Eligibility-Prüfung (`check_strategy_live_eligibility`) für strategiegelabelte Einträge — **Sichtbarkeit**, keine neue Gate-Logik im Builder. | `mode` (`phase83_eligibility_snapshot_v1`), `status`, `source`, `read_only`, `truth_posture`, `tiering_path`, `strategies_checked`, `eligible_count`, `not_eligible_count`, `require_allow_live_flag`, `items[]` (`entity_id`, `is_eligible`, `reasons`, …) | **Hauptseite** — `_render_phase83_eligibility_card`; Überschrift **Phase 83 — Strategy eligibility** (kein separates `id` im aktuellen Bundle); **nicht** in `_render_operator_summary_surface`. Wortlaut im HTML betont Observation und „does not grant live access“. | `tests&#47;webui&#47;test_ops_cockpit.py` — `test_phase83_eligibility_snapshot_*`, `test_ops_cockpit_html_contains_phase83_eligibility_card`; Builder: `_build_phase83_eligibility_snapshot` in `src&#47;webui&#47;ops_cockpit.py`; Quelle in Payload: `src.live.live_gates.check_strategy_live_eligibility` | **Keine** Freigabe, **kein** Live-Unlock, **keine** Änderung von Enforcement; „eligible“ ist **beobachtetes** Ergebnis der vorhandenen Prüfung — **kein** Ersatz für externes Governance &#47; Runbooks. |
| **`workflow_officer_state`** (inkl. **Handoff** &#47; **Next-step preview**) | Sichtbarkeit des **letzten** Workflow-Officer-`report.json` unter `out&#47;ops&#47;workflow_officer` (Panel-Kontext) — bounded **Handoff**- und **Preview**-Zeilen, wenn im Report vorhanden. | Gesamtobjekt von `build_workflow_officer_panel_context`; u. a. `present`, `empty_reason`, `rollup`, `executive`, `primary_followup`, **`handoff_observation`**, **`next_chat_preview_observation`** (Schema-Versionen und Felder wie in Payload) | **`_render_workflow_officer_observation_surface`** — `id=operator-workflow-observation-surface`; Unterblock **Handoff &amp; next-step preview (bounded observation)** — `id=operator-workflow-handoff-preview-observation`. Siehe auch Zeile **Operator workflow (vNext Phase 4)** in [`OPS_COCKPIT_OPERATOR_SUMMARY_SURFACE.md`](OPS_COCKPIT_OPERATOR_SUMMARY_SURFACE.md). | `tests&#47;webui&#47;test_ops_cockpit.py` (Panel &#47; HTML); `tests&#47;ops&#47;test_workflow_officer.py` (Dashboard-View, Handoff &#47; Preview); `tests&#47;test_webui_live_track.py` (Dashboard-Section) | **Visibility-only**; **kein** Cockpit-Start des Officers; **kein** Ersatz für `policy_go_no_go_observation`; **kein** Release-Go-Signal, **keine** Ausführungsautorität. |
| **`update_officer_ui`** | Notifier-/Run-Dir-**Lesesicht** für Update-Officer-Payload (GET-only Ergonomik); leeres &#47; Konfliktmodell deterministisch. | Felder aus `build_update_officer_ui_model` &#47; `build_update_officer_ui_route_conflict` (z. B. `available`, `empty_state_message`, …) | **`_render_update_officer_card`** — `id=update-officer-visibility-card`; **`_render_update_officer_source_ergonomics_block`** unmittelbar **darüber** (Formularparameter `update_officer_*` — **GET**-Navigation, keine Write-Route aus dem Cockpit heraus). Zeile **Update Officer** in [`OPS_COCKPIT_OPERATOR_SUMMARY_SURFACE.md`](OPS_COCKPIT_OPERATOR_SUMMARY_SURFACE.md). | `tests&#47;webui&#47;test_ops_cockpit.py` — `update_officer_ui` &#47; Route-Konflikt &#47; HTML ohne Write-Actions; `tests&#47;test_webui_live_track.py` (API-Client) | **Kein** POST als versteckte Autorität; **kein** Workflow-Officer-Control; nur **Lesbarkeit** lokaler Artefakte. |
| **`balance_semantics_state`** | **Operator-Sicht** auf Bilanz-Semantik aus `LivePortfolioMonitor`&#47;`PaperBroker`-Snapshot (wenn Konfiguration ladbar); steuert **keine** externe Reconciliation. | `balance_semantic_state`, `balance_reason_code`, `balance_operator_visible_state` (können `null` sein, wenn kein Snapshot) | **Hauptseite** — Card **Balance Semantics** in `render_ops_cockpit_html` (Überschrift &#47; Chip-Darstellung); **nicht** als eigener Block in `_render_operator_summary_surface`. Wird **zusätzlich** an andere Leser übergeben: `stale_state.balance` leitet daraus ab; Inputs für `session_end_mismatch_state`, `transfer_ambiguity_state` (siehe Operator-Summary-Tabellen **Session &#47; Run** &#47; **Transfer**). | `tests&#47;webui&#47;test_ops_cockpit.py` — `test_balance_semantics_state_*`, `test_ops_cockpit_html_contains_balance_semantics`; Querverweise in Operator-Summary zu **Stale** &#47; **Transfer** | **Beobachtung** aus lokalem Monitor-Snapshot — **keine** Exchange-Saldo-Wahrheit, **keine** Treasury-Abgleichsfreigabe; **kein** Ersatz für Buchhaltung oder externe Reconciliation. |

## Related

- [`RUNBOOK_OPS_SUITE_DASHBOARD_VNEXT_PLAN.md`](../runbooks/RUNBOOK_OPS_SUITE_DASHBOARD_VNEXT_PLAN.md) — phased plan and Ist-Stand.  
- [`RUNBOOK_OPS_SUITE_PHASE_E_GOVERNANCE_REVIEW.md`](../runbooks/RUNBOOK_OPS_SUITE_PHASE_E_GOVERNANCE_REVIEW.md) — interpretation vs authority.  
- [`docs/ops/registry/DOCS_TRUTH_MAP.md`](../registry/DOCS_TRUTH_MAP.md) — docs drift registry.
