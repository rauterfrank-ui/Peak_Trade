# OPS Cockpit — Operator Summary Surface (vNext-aligned)

**status:** active  
**last_updated:** 2026-04-13  
**purpose:** Map OPS Suite / Dashboard vNext „Required Views“ (Teilmenge) to the JSON payload served at `GET &#47;api&#47;ops-cockpit` (same shape as the `/ops` HTML page) and HTML render helpers — read-only, no execution authority.

**docs_token:** `DOCS_TOKEN_OPS_COCKPIT_OPERATOR_SUMMARY_SURFACE`

## Non-Goals

- No live unlock, no approval semantics, no gate weakening.
- No new API routes; existing payload keys keep the same meaning. Additive **read-only** keys (e.g. `workflow_officer_state`) are allowed when they surface existing artifacts with no new authority.

## Phase E — Operator interpretation (governance, read-only)

This surface maps payload keys and HTML blocks for **visibility** only. **Phase E** closure is documented in [`RUNBOOK_OPS_SUITE_PHASE_E_GOVERNANCE_REVIEW.md`](../runbooks/RUNBOOK_OPS_SUITE_PHASE_E_GOVERNANCE_REVIEW.md) (traceability to this file, the payload contract, and tests).

**Separation operators should use:**

| Layer | Payload examples | Read as |
|-------|-------------------|---------|
| Policy / guard posture | `policy_state`, `guard_state`, `operator_state` | Config and local state-file observation in the builder — **not** an external approval or live mandate. |
| Compact observations | `*_observation` keys | Conservative rollups — **not** unlock, **not** broker truth. |
| Workflow / notifier tooling | `workflow_officer_state`, `update_officer_ui` | Artifact/notifier visibility — **not** a substitute for `policy_go_no_go_observation` or go/no-go approval. |

**Non-claims:** The Cockpit does not assert exchange reconciliation correctness, session enforcement on brokers, or audit sign-off. Resolve ambiguities with external governance and runbooks, not by inferring authority from this page alone.

## Required View ↔ Payload ↔ Render

| vNext Required View (Auszug) | Payload keys (observation) | HTML / helper |
|------------------------------|------------------------------|----------------|
| System State | `system_state.mode`, `system_state.execution_model`, `system_state.config_load_status` (`not_loaded` / `loaded` / `unavailable`), `system_state.environment` (config observation; `unknown` when not loaded), `system_state.bounded_pilot_mode` (when config loaded; else `null`), `system_state.gating_posture_observation` (mirror of `policy_state.summary` at build time); optional Kurzbezug: `dependencies_state.summary`, `evidence_state.summary` | `src/webui/ops_cockpit.py` — `_render_operator_summary_surface`, section **System status (observation)**; hero line **Config environment (observation)** in `render_ops_cockpit_html` |
| System / environment (compact observation) | `system_state_observation` — `status`, `summary`, `data_source` (`cockpit_payload_aggregate`), `reader_schema_version`, `consistency_notes`, `contributing_signals`, `provenance`, optional `observation_reason`; aggregate from existing `system_state`, optional consistency check vs. `policy_state.summary` — **not** broker or exchange truth, **not** an environment guarantee; separate from `health_drift_observation`, `safety_posture_observation`, and `policy_go_no_go_observation` | Same — subsection **System / environment (observation)** (`id=operator-summary-system-state-observation`; `_render_operator_summary_system_state_observation`; after inline **System status** lines; before **Phase 83 — Strategy eligibility (observation)** and **Go / No-Go observation (not approval)**) |
| Supplementary — Phase 83 strategy eligibility | `phase83_eligibility_snapshot` — compact lines: `status`, `strategies_checked`, `eligible_count`, `not_eligible_count`, `read_only`, `truth_posture`, `require_allow_live_flag`; same builder as main-page card — **not** live approval, **not** gate weakening | Same — **Phase 83 — Strategy eligibility (observation)** (`id=operator-summary-phase83-eligibility`; after **System / environment (observation)** when present; before **Go / No-Go observation (not approval)**) |
| Go / No-Go (observation, not approval) | `policy_state.action`, `policy_state.blocked`, `incident_state.summary`, `incident_state.requires_operator_attention`, kill-switch flags in payload | Same — section **Go / No-Go observation (not approval)** |
| Policy / go-no-go (compact observation) | `policy_go_no_go_observation` — `status`, `summary`, `data_source` (`cockpit_payload_aggregate`), `reader_schema_version`, `consistency_notes`, `contributing_signals`, `provenance`, optional `observation_reason`; aggregate from existing `policy_state`, `incident_state`, and `operator_state` only — **not** a live go decision, **not** approval or unlock, **not** broker truth; separate from `safety_posture_observation` (holistic gating posture) | Same — subsection **Policy / go-no-go (observation)** (`id=operator-summary-policy-go-no-go-observation`; `_render_operator_summary_policy_go_no_go_observation`; after inline Go/No-Go lines; before **Operator state (payload)** and **Safety / gating posture (observation)**) |
| Supplementary — Operator state (payload snapshot) | `operator_state` — `disabled`, `enabled`, `armed`, `dry_run`, `blocked`, `kill_switch_active` when present — same top-level object as in the JSON payload; **not** approval or unlock, **not** a substitute for `policy_go_no_go_observation` | Same — **Operator state (payload)** (`id=operator-summary-operator-state`; `_render_operator_summary_operator_state`; after **Policy / go-no-go (observation)**; before **Safety / gating posture (observation)**) |
| Safety / gating posture (compact observation) | `safety_posture_observation` — `status`, `summary`, `data_source` (`cockpit_payload_aggregate`), `reader_schema_version`, `consistency_notes`, `contributing_signals`, `provenance`, optional `observation_reason`; aggregate at build time from existing `policy_state`, `guard_state`, `incident_state`, `operator_state`, `system_state`, `stale_state`, `dependencies_state` only — **not** approval, **not** unlock, **not** broker truth | Same — section **Safety / gating posture (observation)** (`id=operator-summary-safety-posture-observation`; `_render_operator_summary_safety_posture_observation`; after **Operator state (payload)**; before **Safety state (vNext projection)**) |
| vNext **safety_state** (top-level projection) | `safety_state` — `summary`, `data_source` (`cockpit_payload_aggregate`), `reader_schema_version`, `provenance`, `posture_observation` (status + schema ref from `safety_posture_observation`), `incident_safety_observation` (status + schema ref from `incident_safety_observation`), `incident_signal_subset` (scalar rollups from `incident_state` only) — `src&#47;ops&#47;safety_state.py` (`build_safety_state`); **not** new gating logic, **not** approval, **not** unlock; same semantics as [`OPS_COCKPIT_PAYLOAD_READ_MODEL_CONTRACT.md`](OPS_COCKPIT_PAYLOAD_READ_MODEL_CONTRACT.md) | Same — section **Safety state (vNext projection)** (`id=operator-summary-safety-state-projection`; after **Safety / gating posture (observation)**); renders `summary`, `data_source`, `reader_schema_version`, optional `provenance.observation_reason`, and defensive projection refs when present |
| Governance / AI boundary (compact observation) | `governance_boundary_observation` — `status`, `summary`, `data_source` (`cockpit_payload_aggregate`), `reader_schema_version`, `consistency_notes`, `contributing_signals`, `provenance`, optional `observation_reason`; aggregate from existing `ai_boundary_state` and `human_supervision_state`, optional hints from `guard_state` / `evidence_state` — **not** governance or AI approval, **not** supervision waiver, **not** model certification; separate from `safety_posture_observation` and `evidence_audit_observation` | Same — section **Governance / AI boundary (observation)** (`id=operator-summary-governance-boundary-observation`; `_render_operator_summary_governance_boundary_observation`; after **Safety state (vNext projection)**; before **Policy / Governance (vNext RV6) — compact observation** (`id=operator-summary-policy-governance-rv6`) and **Operator workflow (observation)**); distinct from **Policy / go-no-go (observation)** and from the RV6 compact mirror |
| Supplementary — Operator workflow (Phase 4) | `workflow_officer_state` — compact: `present`, `empty_reason`, optional `rollup` (`total_checks`, `hard_failures`, `warnings`), optional `executive.urgency_label`, optional `primary_followup.check_id` — same object as full **Operator workflow observation** card; **not** approval, **not** release go-signal | Same — **Operator workflow (observation)** (`id=operator-summary-workflow-officer`; after **Governance / AI boundary (observation)**; before **Update Officer (observation)**) |
| Supplementary — Update Officer (notifier tooling) | `update_officer_ui` — compact: `available`, `empty_state_message` when unavailable; when available optional `headline`, `status`, `next_topic`, `severity`, `reminder_class`, `requires_manual_review` — same object as **Update Officer** card (`build_update_officer_ui_model` / empty-state); **not** approval, **not** unlock, **not** Workflow Officer start | Same — **Update Officer (observation)** (`id=operator-summary-update-officer`; after **Operator workflow (observation)**; before **Live status snapshot (Phase 57) — endpoints** and **Run / session (observation)**) |
| Supplementary — Phase 57 live snapshot (endpoint discoverability) | (no extra payload keys) — **GET** links to existing snapshot JSON/HTML routes only — **not** live approval, **not** go-live claim | Same — **Live status snapshot (Phase 57) — endpoints** (`id=operator-summary-phase57-snapshot-discoverability`; `_render_operator_summary_phase57_snapshot_discoverability`; after **Update Officer (observation)**; before **Run / session (observation)**); mirrors `_render_phase57_snapshot_discoverability_card` link targets on the main page |
| Session / Run (compact observation) | `run_session_observation` — `status`, `summary`, `data_source` (`cockpit_payload_aggregate`), `reader_schema_version`, `consistency_notes`, `contributing_signals`, `provenance`, optional `observation_reason`; aggregate from existing `run_state`, `session_end_mismatch_state`, `stale_state`, optional `operator_state` — **not** a session guarantee, **not** approval, **not** exchange session truth | Same — section **Run / session (observation)** (after **Update Officer (observation)** and Phase 57 discoverability when present; before **Run state (observation)** supplementary) |
| Supplementary — Run state (observed rollup) | `run_state` — `status`, `active`, `last_run_status`, `session_active`, optional `registry_session_count`, `registry_last_started_at`, `generated_at`, `freshness_status` — same field subset as **Run state — observed rollup** card (`_render_run_state_observation_card`); **not** start/stop control, **not** execution authority, **not** go/no-go | Same — **Run state (observation)** (`id=operator-summary-run-state`; after **Run / session (observation)**; before **Session end mismatch (observation)** supplementary) |
| Supplementary — Session end mismatch (read model) | `session_end_mismatch_state` — `status`, `summary`, `blocked_next_session`, `data_source`, `observation_reason`, `reader_schema_version`, `runbook` — same builder as **Session End Mismatch** card; **not** broker enforcement on sessions, **not** approval, **not** unlock | Same — **Session end mismatch (observation)** (`id=operator-summary-session-end-mismatch`; after **Run state (observation)** supplementary; before **Transfer / treasury ambiguity (observation)** supplementary) |
| Supplementary — Transfer / treasury ambiguity (read model) | `transfer_ambiguity_state` — `status`, `summary`, `data_source`, `observation_reason`, `runbook_ref`, `operator_attention_hint`, `reader_schema_version` — same builder as **Transfer / Treasury ambiguity** card; **not** exchange transfer truth, **not** approval, **not** all-clear | Same — **Transfer / treasury ambiguity (observation)** (`id=operator-summary-transfer-ambiguity`; after session-end supplementary; before **Stale signals (observation)**) |
| Supplementary — Stale signals (scalar rollup) | `stale_state` — `summary`, `balance`, `position`, `order`, `exposure` — local reconciliation / recency labels; **`order`** is event-log recency for `live_runs` when derivable (`order_staleness_reader` path in builder) — **not** exchange order-book state; same semantics as **Stale State** card | Same — **Stale signals (observation)** (`id=operator-summary-stale-signals`; after **Transfer / treasury ambiguity (observation)** supplementary; before **Health / drift (observation)** (`id=operator-summary-health-drift-observation`)) |
| Health / Drift (compact observation) | `health_drift_observation` — `status`, `summary`, `data_source` (`cockpit_payload_aggregate`), `reader_schema_version`, `consistency_notes`, `contributing_signals`, `provenance`, optional `observation_reason`; aggregate from top-level `truth_status` / `freshness_status` / `source_coverage_status`, `critical_flags`, `unknown_flags`, `evidence_state`, `dependencies_state`, `stale_state`, optional nested `executive_summary` for consistency checks — **not** a live service health guarantee, **not** approval, **not** broker truth | Same — section **Health / drift (observation)** (`id=operator-summary-health-drift-observation`; `_render_operator_summary_health_drift_observation`; after **Stale signals (observation)** when present) |
| Supplementary — Dependencies artifact observations | `dependencies_state.p85_exchange_observation`, `dependencies_state.market_data_cache_observation` — compact lines for keys present on each object (e.g. P85: `data_source`, `stale`, `observation_reason`, `reader_schema_version`, `last_updated_utc`, `artifact_path`; cache: `data_source`, `observation_reason`, `reader_schema_version`, `last_updated_utc`, `market_data_cache`) — **not** a live feed check, **not** exchange or broker truth, **not** approval; same semantics as the Dependencies State card (`_render_dependencies_state_card_body`) | Same — **Dependencies — artifact observations (read-only)** (`id=operator-summary-dependencies-artifact-observations`; after **Health / drift (observation)** (`id=operator-summary-health-drift-observation`); before **Exposure / risk (observation)** (`id=operator-summary-exposure-risk-observation`)) |
| Exposure / Risk (compact observation) | `exposure_risk_observation` — `status`, `summary`, `data_source` (`cockpit_payload_aggregate`), `reader_schema_version`, `consistency_notes`, `contributing_signals`, `provenance`, optional `observation_reason`; aggregate at build time from existing `exposure_state`, `transfer_ambiguity_state`, `stale_state`, and `guard_state` (treasury mirror) only — **not** broker or exchange truth, **not** a risk approval or cap unlock, **not** net exposure authority | Same — section **Exposure / risk (observation)** (`id=operator-summary-exposure-risk-observation`; `_render_operator_summary_exposure_risk_observation`; after Health / drift and **Dependencies — artifact observations**; before **Exposure state (observation)** supplementary) |
| Supplementary — Exposure state (read model) | `exposure_state` — scalars such as `summary`, `treasury_separation`, `risk_status`, `observed_exposure`, `observed_ccy`, `data_source`, `last_updated_utc`, `stale`; optional compact counts for `caps_configured` and `exposure_by_symbol` when non-empty — same object as **Exposure State** card; **not** broker or exchange truth, **not** approval or unlock | Same — **Exposure state (observation)** (`id=operator-summary-exposure-state`; after **Exposure / risk (observation)** (`id=operator-summary-exposure-risk-observation`); before **Balance semantics (observation)**) |
| Supplementary — Balance semantics (local snapshot) | `balance_semantics_state` — `balance_semantic_state`, `balance_reason_code`, `balance_operator_visible_state` (nullable when no snapshot) — same builder object as **Balance Semantics** card; **not** exchange reconciliation truth, **not** approval or unlock | Same — section **Balance semantics (observation)** (`id=operator-summary-balance-semantics`; after **Exposure state (observation)** supplementary; before **Incident / safety (observation)** (`id=operator-summary-incident-safety-observation`)) |
| Incident / Safety (compact observation) | `incident_safety_observation` — `status`, `summary`, `data_source` (`cockpit_payload_aggregate`), `reader_schema_version`, `consistency_notes`, `contributing_signals`, `provenance`, optional `observation_reason`; aggregate from existing `incident_state` and `dependencies_state`, optional consistency hints from `policy_state` &#47; `operator_state` — **not** incident resolution, **not** approval or unlock, **not** exchange truth, **not** an all-clear; narrower than `safety_posture_observation` (holistic gating posture) | Same — section **Incident / safety (observation)** (`id=operator-summary-incident-safety-observation`; `_render_operator_summary_incident_safety_observation`; after Exposure / risk and **Balance semantics**; before detailed **Incident observation (read-only)**) |
| Incident / Safety (detail lines) | `incident_state.status`, `incident_state.degraded`, `incident_state.requires_operator_attention`, `incident_state.incident_stop_invoked`, `incident_state.entry_permitted`, `incident_state.operator_authoritative_state`; `dependencies_state.summary`, `dependencies_state.telemetry`, `dependencies_state.exchange`, `dependencies_state.degraded` | Same — section **Incident observation (read-only)** |
| Evidence / Audit (compact observation) | `evidence_audit_observation` — `status`, `summary`, `data_source` (`cockpit_payload_aggregate`), `reader_schema_version`, `consistency_notes`, `contributing_signals`, `provenance`, optional `observation_reason`; aggregate from existing `evidence_state`, optional consistency hints from top-level `truth_status` / `freshness_status` and nested `executive_summary` — **not** audit clearance, **not** compliance pass/fail, **not** approval; separate from `health_drift_observation` (service/drift posture) | Same — section **Evidence / audit (observation)** (`id=operator-summary-evidence-audit-observation`; `_render_operator_summary_evidence_audit_observation`; after **Incident observation (read-only)**; before **Evidence freshness observation (read-only)**) |
| Evidence / Freshness (detail lines) | `evidence_state.summary`, `evidence_state.freshness_status`, `evidence_state.audit_trail`, `evidence_state.last_verified_utc`, `evidence_state.source_freshness`, optional `evidence_state.telemetry_evidence` | Same — section **Evidence freshness observation (read-only)** |
| Supplementary — Truth pipeline (`truth_state`) | `truth_state` — `last_verified_utc`, `truth_coverage`, `available_count`, `unavailable_count`, `truth_first_positioning`, `final_trade_authority`, `live_autonomy` when present — from `build_truth_state`; **not** broker or exchange truth, **not** a live service guarantee | Same — **Truth pipeline (payload)** (`id=operator-summary-truth-state`; `_render_operator_summary_truth_state`; immediately before **Truth sources &amp; runtime** and **Status at a glance**); does **not** replace v3 executive rollups in **Status at a glance** or `health_drift_observation` |
| Supplementary — Truth sources &amp; runtime | `runtime_unknown_state` (scalar labels); `source_groups` (per-group doc lists — sizes only in summary); `source_group_summary` (per-group `total` / `available` / `unavailable` rollups); `canonical_sources` (count of truth-doc inventory entries only) — **not** broker or exchange truth, **not** a live runtime guarantee | Same — **Truth sources &amp; runtime (payload)** (`id=operator-summary-truth-sources-runtime`; `_render_operator_summary_truth_sources_runtime`; after **Truth pipeline (payload)**, before **Status at a glance**); compact counts/labels only — **not** the full main-page truth UI |
| Kompakte Rollups (Truth / Freshness / Sources) | `executive_summary` (nested levels/labels), top-level `truth_status` / `freshness_status` / `source_coverage_status`, `critical_flags`, `unknown_flags` | `_render_status_at_a_glance_inner` (Status-at-a-glance cards) |
| Policy &#47; Governance (vNext RV6) | `policy_state`, `guard_state`, `ai_boundary_state`, `human_supervision_state`; evidence/audit cross-ref from `evidence_state` (full card below) | `_render_policy_governance_observation_surface` — block **Policy &#47; Governance observation (vNext RV6)** (`id=policy-governance-observation-surface`); Evidence State card `id=evidence-state-card`. **Operator Summary** — `_render_operator_summary_policy_governance_rv6`, Abschnitt **Policy &#47; Governance (vNext RV6) — compact observation** (`id=operator-summary-policy-governance-rv6`; kompakte Spiegelung derselben Payload-Objekte; Verweis auf Detail-Surface und Evidence-Karte) |
| Operator workflow (vNext Phase 4) | `workflow_officer_state` (from `build_workflow_officer_panel_context` &#47; latest `out&#47;ops&#47;workflow_officer` `report.json` when present); additive bounded **`workflow_officer_state.handoff_observation`** and **`workflow_officer_state.next_chat_preview_observation`** when `summary` in that JSON includes `handoff_context` &#47; `next_chat_preview` | `_render_workflow_officer_observation_surface` — block **Operator workflow observation (vNext Phase 4)** (`id=operator-workflow-observation-surface`); subsection **Handoff &amp; next-step preview (bounded observation)** (`id=operator-workflow-handoff-preview-observation`). Read-only copy stresses: not approval, not unlock, not execution authority, not a release go-signal; **`policy_go_no_go_observation`** remains the compact policy line (separate payload object). |
| Update Officer (read-only visibility) | `update_officer_ui` — notifier-derived lines when a local payload path resolves (`build_update_officer_ui_model`); route conflict surfaces an empty model with a deterministic message | `_render_update_officer_card` — **`id=update-officer-visibility-card`** (after Dependencies in `render_ops_cockpit_html`); GET-only **Update Officer source ergonomics** (`_render_update_officer_source_ergonomics_block`) render **immediately above** the card on the same page. **Operator Summary** — compact **Update Officer (observation)** (`id=operator-summary-update-officer`; see supplementary row above). **Non-Goals:** no POST, no Workflow Officer start, no hidden execution authority. |

### Exposure / Risk (separate card, read-only)

Presentation-only **Exposure State** card in `render_ops_cockpit_html` — observation of existing keys; not approval, not unlock. Cross-check: [`OPS_SUITE_EXPOSURE_READ_MODEL_CONTRACT.md`](OPS_SUITE_EXPOSURE_READ_MODEL_CONTRACT.md).

| Observation | Payload keys (read from `exposure_state` unless noted) |
|-------------|--------------------------------------------------------|
| Summary, treasury, risk | `summary`, `treasury_separation`, `risk_status` |
| Observed values | `observed_exposure`, `observed_ccy`, `data_source`, `last_updated_utc`, `stale` |
| Cross-surface context | `stale_state.exposure` (from `stale_state`), `dependencies_state.summary` (from `dependencies_state`) |
| Caps & breakdown | `caps_configured`, `exposure_by_symbol` (preview) |

### Dependencies / Health-Drift (separate card, read-only)

Presentation-only **Dependencies State** card — `_render_dependencies_state_card_body` in `render_ops_cockpit_html`; existing keys only; not approval, not unlock. Data-quality context: [`OPS_SUITE_DEPENDENCIES_STATE_DATA_QUALITY_REVIEW.md`](OPS_SUITE_DEPENDENCIES_STATE_DATA_QUALITY_REVIEW.md).

| Observation | Payload keys (`dependencies_state`) |
|-------------|--------------------------------------|
| Rollup | `summary`, `exchange`, `telemetry` |
| P85 artifact observation (read-only) | `p85_exchange_observation` — `reader_schema_version`, `exchange` (duplicate of rollup for self-contained object), `data_source` (`p85_result_json` / `none`), `artifact_path`, `last_updated_utc` (artifact mtime), `stale`, `observation_reason`, `provenance`; newest `P85_RESULT.json` under `out&#47;ops&#47;**` by mtime; **not** a live connectivity check from the Cockpit build — see [`OPS_SUITE_DEPENDENCIES_STATE_DATA_QUALITY_REVIEW.md`](OPS_SUITE_DEPENDENCIES_STATE_DATA_QUALITY_REVIEW.md) |
| Local market-data cache observation (read-only) | `market_data_cache_observation` — `reader_schema_version`, `market_data_cache` (rollup mirror), `data_source` (`kraken_parquet_cache_local` / `none`), `observation_reason`, `provenance`, `details`, `last_updated_utc` (data end hint when present); offline `check_data_health_only` on `real_market_smokes.base_path` from config — **not** a live feed check — see [`OPS_SUITE_DEPENDENCIES_STATE_DATA_QUALITY_REVIEW.md`](OPS_SUITE_DEPENDENCIES_STATE_DATA_QUALITY_REVIEW.md) |
| Optional feed health | `market_data_cache` (shown as `n&#47;a` when absent) |
| Degraded checklist | `degraded` (list preview) |

### Evidence / Audit (separate card, read-only)

Presentation-only **Evidence State** card — `_render_evidence_state_card_body` in `render_ops_cockpit_html`; same payload keys as the compact operator-summary block; not approval, not unlock.

| Observation | Payload keys (`evidence_state`) |
|---------------|----------------------------------|
| Rollup | `summary`, `freshness_status`, `audit_trail`, `last_verified_utc` |
| Source counts | `source_freshness` (`fresh` / `stale` / `older`) |
| Optional | `telemetry_evidence` (shown as `n&#47;a` when absent in payload) |

### Session / Run / Stale (cards + rollup, read-only)

Maps vNext **Session / Run State** and parts of **Health / Drift** to existing payload objects — observation only; not approval, not unlock. PR/CI context for feature branches: [`RUNBOOK_PR_CI_VERIFICATION.md`](../runbooks/RUNBOOK_PR_CI_VERIFICATION.md).

| Observation | Payload keys | HTML / helper |
|-------------|----------------|---------------|
| Run / session rollup | `run_state.status`, `run_state.active`, `run_state.last_run_status`, `run_state.session_active`, optional `run_state.registry_session_count`, `run_state.registry_last_started_at` (when live session registry present), `run_state.generated_at`, `run_state.freshness_status` (subset on page) | `_render_run_state_observation_card` |
| Stale / reconciliation | `stale_state.summary`, `stale_state.balance`, `stale_state.position`, `stale_state.order`, `stale_state.exposure` | **Stale State** card in `render_ops_cockpit_html` |
| Session end (read model) | `session_end_mismatch_state.status`, `summary`, `blocked_next_session` (observation hint only, not enforcement), `runbook`, `data_source`, `observation_reason`, `reader_schema_version`, `provenance` | **Session End Mismatch** card (`id=session-end-mismatch-observation-surface`) in `render_ops_cockpit_html`; builder: `build_session_end_mismatch_state` in `src/live/session_end_mismatch_reader.py` — **local** registry + `live_runs` metadata/events only; not broker truth |
| Transfer / treasury ambiguity (read model) | `transfer_ambiguity_state.status`, `summary`, `data_source`, `observation_reason`, `runbook_ref`, `operator_attention_hint`, `reader_schema_version`, `provenance` | **Transfer / Treasury ambiguity** card (`id=transfer-ambiguity-observation-surface`) in `render_ops_cockpit_html`; builder: `build_transfer_ambiguity_state` in `src/live/transfer_ambiguity_reader.py` — aggregates **local** cockpit signals (`guard_state.treasury_separation`, `stale_state.balance`, `balance_semantics_state`, `exposure_state.stale`); optional process-env key role in `provenance` only; **not** broker transfer truth, **not** approval or unlock — see [`RUNBOOK_PILOT_INCIDENT_TRANSFER_AMBIGUITY.md`](../runbooks/RUNBOOK_PILOT_INCIDENT_TRANSFER_AMBIGUITY.md) |

**Note — `stale_state.order`:** When non-empty `live_runs&#47;*&#47;events.*` exist, `stale_state.order` is derived read-only via `get_live_runs_order_staleness` in `src/live/order_staleness_reader.py` (event log **mtime** vs age threshold; same default window as exposure). Values: **`ok`** (recent logs), **`stale`** (old logs), **`unknown`** (no qualifying runs/events). This is **not** exchange order-book state — Stale State card includes an explicit disclaimer line.

**Note — `session_end_mismatch_state`:** Status values are conservative (e.g. `unknown` / `no_signal`, `aligned`, `ambiguous`, `mismatch_signal`). When registry JSONs under `reports&#47;experiments&#47;live_sessions` and/or `live_runs` metadata exist, the reader cross-checks closure hints; it does **not** call brokers or unlock sessions.

**Note — `transfer_ambiguity_state`:** Conservative statuses (`ok`, `warning`, `unknown`, `no_signal`); missing or partial local signals must not read as implicit all-clear. Does **not** assert transfer completion or exchange balances.

**Note — `dependencies_state.exchange` (P85):** Populated from **persisted** P85 ingest-readiness artifacts only (`read_p85_exchange_observation` in `src/ops/p85_result_reader.py`). Stale artifacts (default age threshold 3600s) map to `unknown`. Does **not** call exchanges during payload build.

**Note — `market_data_cache_observation`:** Local parquet / filesystem QC only (`read_market_data_cache_observation` in `src/ops/market_data_cache_observation_reader.py`). Does **not** assert live market data or broker truth.

## Related

- [`OPS_COCKPIT_VNEXT_REQUIRED_VIEWS_COVERAGE.md`](OPS_COCKPIT_VNEXT_REQUIRED_VIEWS_COVERAGE.md) — vNext Required Views §1–7 ↔ payload / summary surface / tests (compact traceability); **§ Supplementary Cockpit surfaces** für repo-vorhandene Keys **außerhalb** der Required-Views-Matrix (z. B. `phase83_eligibility_snapshot`, `workflow_officer_state`, `update_officer_ui`, Phase 57 snapshot discoverability, `balance_semantics_state`) — gleiche Traceability-Tiefe **ohne** RV-Neuzuordnung.
- [`OPS_COCKPIT_PAYLOAD_READ_MODEL_CONTRACT.md`](OPS_COCKPIT_PAYLOAD_READ_MODEL_CONTRACT.md) — canonical top-level payload key contract for `build_ops_cockpit_payload` (read-model; not UI layout).
- [`OPS_SUITE_DASHBOARD_VNEXT_SPEC.md`](OPS_SUITE_DASHBOARD_VNEXT_SPEC.md) — operator-facing target spec.
- [`RUNBOOK_OPS_SUITE_DASHBOARD_VNEXT_PLAN.md`](../runbooks/RUNBOOK_OPS_SUITE_DASHBOARD_VNEXT_PLAN.md) — phased plan; RV6 Policy/Governance and Phase 4 Workflow Officer observation surfaces shipped read-only (HTML bundle).
- [`RUNBOOK_OPS_SUITE_PHASE_E_GOVERNANCE_REVIEW.md`](../runbooks/RUNBOOK_OPS_SUITE_PHASE_E_GOVERNANCE_REVIEW.md) — Phase E governance review; interpretation vs authority; canonical anchors.
- [`RUNBOOK_PR_CI_VERIFICATION.md`](../runbooks/RUNBOOK_PR_CI_VERIFICATION.md) — PR/CI events and verification (truth-first).

## Code references

- Payload builder: `build_ops_cockpit_payload` in `src/webui/ops_cockpit.py`
- Session-end observation: `build_session_end_mismatch_state` in `src/live/session_end_mismatch_reader.py`
- Transfer / treasury ambiguity observation: `build_transfer_ambiguity_state` in `src/live/transfer_ambiguity_reader.py`
- P85 exchange observation: `read_p85_exchange_observation` in `src/ops/p85_result_reader.py`
- Market data cache observation: `read_market_data_cache_observation` in `src/ops/market_data_cache_observation_reader.py`
- Safety / gating posture observation (aggregate): `build_safety_posture_observation` in `src/ops/safety_posture_observation.py`; operator summary section: `_render_operator_summary_safety_posture_observation` in `src/webui/ops_cockpit.py`
- Run / session observation (aggregate): `build_run_session_observation` in `src/ops/run_session_observation.py`
- Health / drift observation (aggregate): `build_health_drift_observation` in `src/ops/health_drift_observation.py`
- Exposure / risk observation (aggregate): `build_exposure_risk_observation` in `src/ops/exposure_risk_observation.py`
- Incident / safety observation (aggregate): `build_incident_safety_observation` in `src/ops/incident_safety_observation.py`
- Evidence / audit observation (aggregate): `build_evidence_audit_observation` in `src/ops/evidence_audit_observation.py`
- Governance / AI boundary observation (aggregate): `build_governance_boundary_observation` in `src/ops/governance_boundary_observation.py`; operator summary section: `_render_operator_summary_governance_boundary_observation` in `src/webui/ops_cockpit.py`
- Policy / go-no-go observation (aggregate): `build_policy_go_no_go_observation` in `src/ops/policy_go_no_go_observation.py`; operator summary section: `_render_operator_summary_policy_go_no_go_observation` in `src/webui/ops_cockpit.py`
- System / environment observation (aggregate): `build_system_state_observation` in `src/ops/system_state_observation.py`; operator summary section: `_render_operator_summary_system_state_observation` in `src/webui/ops_cockpit.py`
- HTML entry: `render_ops_cockpit_html` in `src/webui/ops_cockpit.py`
- Tests: `tests/webui/test_ops_cockpit.py`, `tests/ops/test_ops_cockpit_payload_top_level_contract.py`, `tests/ops/test_safety_posture_observation.py`, `tests/ops/test_policy_go_no_go_observation.py`, `tests/ops/test_system_state_observation.py`, `tests/ops/test_run_session_observation.py`, `tests/ops/test_health_drift_observation.py`, `tests/ops/test_exposure_risk_observation.py`, `tests/ops/test_incident_safety_observation.py`, `tests/ops/test_evidence_audit_observation.py`, `tests/ops/test_governance_boundary_observation.py`, `tests/live/test_session_end_mismatch_reader.py`, `tests/live/test_transfer_ambiguity_reader.py`, `tests/ops/test_p85_result_reader.py`, `tests/ops/test_market_data_cache_observation_reader.py`
