# RUNBOOK_PATCH_RECOMMENDATIONS

> **Status:** advisory only — **do not** silent-patch the Product Runbook.  
> **Generated by:** `GO_PEAK_TRADE_VISUAL_OPERATOR_DASHBOARD_RUNBOOK_REPOSITORY_BOOTSTRAP_V1` Phase 0B  
> **Compared against:** Runbook Discovery Baseline §0A + current worktree  
> **Checked HEAD:** `20969b4a155ffbdc0e1a9a55657311aa061511be`  
> **origin/main:** `e20b788c67c4955f1e25903cb9913c860e7a7452`

```text
DOCUMENT_ROLE=NON_CANONICAL_RECOMMENDATION_LOG
MAY_NOT_OVERRIDE_RUNBOOK=true
PATCHES_REQUIRE_SEPARATE_RATIFICATION=true
SILENT_RUNBOOK_MUTATION=false
PATCH_MODE=RECOMMENDATION_LIST_ONLY
```

Dieses Dokument ist ausschließlich eine Gap-/Patch-Liste. Es ist nicht kanonisch und darf das Product Runbook nicht überschreiben. Ratifizierte Korrekturen benötigen einen eigenen Auftrag (dieser Commit-Slice ratifiziert nur die explizit genannten Dokumentkorrekturen A–D).

---

## Traceability summary

| Check | Result |
|-------|--------|
| Discovery HEAD matches current feature HEAD | PASS (`20969b4…`) |
| Discovery branch matches | PASS (`feat/market-dashboard-visual-operator-surface-v1`) |
| Discovery PR exists | PASS (PR [#5244](https://github.com/rauterfrank-ui/Peak_Trade/pull/5244) OPEN) |
| Worktree clean at check | PASS |
| Primary route/owners/paths exist | PASS (see inventory) |
| All Discovery claims still exact | FAIL — deltas listed below |
| `selection_context_id` present in repo | FAIL — not found under `src/webui/` |

---

## Owner / path inventory (verified present)

| Discovery claim | Repo path | Status |
|-----------------|-----------|--------|
| `DASHBOARD_ROUTE=&#47;market` | `src/webui/app.py` → `include_router(create_market_router(...))` | PRESENT |
| `create_market_router` | `src/webui/market_surface.py` | PRESENT |
| `resolve_market_page_data` | `src/webui/market_surface.py` | PRESENT |
| `build_market_v0_page_template_context` | `src/webui/market_surface.py` | PRESENT |
| `market_dashboard_current_state_snapshot_v0` | `src/webui/market_dashboard_current_state_snapshot_v0.py` | PRESENT |
| `PRIMARY_TEMPLATE` | `templates/peak_trade_dashboard/market_v0.html` | PRESENT |
| Visual operator surface package | `src/webui/market_visual_operator_surface_v1/` | PRESENT (package; Discovery text implies module symbol only) |
| Header / hero / chart / ranking / funnel / economic / linear / F5 / DP / safety / watchlist / current state / diagnostics partials | `templates&#47;peak_trade_dashboard&#47;partials&#47;*` listed in Runbook §0C | ALL PRESENT |
| OHLCV / Ranking runtime owners | `market_futures_ohlcv_runtime_v0.py`, `market_ranking_funnel_runtime_v0.py` | PRESENT |
| Decision / Economic / Linear display adapters | `market_visual_operator_surface_v1&#47;{decision_funnel,economic_observability,ai_linear_diagnostics}_display_v1.py` | PRESENT |
| Technical surface docs | `docs/webui/MARKET_SURFACE_V0.md` | PRESENT |
| Futures display contract | `docs/ops/specs/FUTURES_READ_ONLY_MARKET_DASHBOARD_CONTRACT_V0.md` | PRESENT |
| Tailwind CDN | `templates/peak_trade_dashboard/base.html` | PRESENT (matches D9 target-state debt) |
| Primary chart renderer SSR SVG | `partials&#47;market_primary_close_chart_v1.html` | PRESENT |
| Chart.js 4.4.1 | vendor path + CDN still used on detail/legacy panels | PRESENT (matches D9) |

---

## Recommended patches (do not apply silently)

### R1 — Clarify SSOT ownership split vs `MARKET_SURFACE_V0.md`

| Field | Value |
|-------|-------|
| Severity | HIGH (docs governance) |
| Finding | `docs/webui/MARKET_SURFACE_V0.md` still describes itself as canonical product/contract / Repo-SSOT for Market Surface. The Product Runbook is now the **Product SSOT + Implementation Spec** for the Visual Operator Dashboard. |
| Risk if ignored | Dual “product truth” confusion; agents mutate wrong owner. |
| Recommended patch | Add an explicit ownership split in Runbook §0A (and keep a pointer in `docs/product/README.md`): Product Runbook = product/UX/impl; `MARKET_SURFACE_V0.md` = technical route/marker/env chronicle; core semantics remain MASTER_V2. Do **not** delete or rewrite `MARKET_SURFACE_V0.md` claims in the same slice without a dedicated docs GO. |

### R2 — Refresh Cursor Master GO token version string

| Field | Value |
|-------|-------|
| Severity | LOW |
| Finding | Runbook §13 previously said `GO_PEAK_TRADE_VISUAL_OPERATOR_DASHBOARD_PRODUCT_V1_2` while the document title is **v1.3**. |
| Status | **CLOSED in docs commit slice** `GO_VISUAL_OPERATOR_DASHBOARD_RUNBOOK_BOOTSTRAP_COMMIT_AND_PR5244_UPDATE_V1` — corrected to `…_PRODUCT_V1_3`. |

### R3 — Document `selection_context_id` as not-yet-implemented

| Field | Value |
|-------|-------|
| Severity | HIGH (integrity contract) |
| Finding | Runbook §0E requires `selection_context_id`. Grep under `src/webui/` finds **no** symbol. |
| Status | **DOCUMENTED (not implemented)** in Runbook §0E via `SELECTION_CONTEXT_IMPLEMENTED=false` / `SELECTION_CONTEXT_REQUIRED_BEFORE_PHASE_4C_COMPLETE=true`. Closure remains Phase **4C**. |

### R4 — Reconcile default instrument / source narrative with Futures-only product rules

| Field | Value |
|-------|-------|
| Severity | HIGH (product boundary) |
| Finding | `MARKET_SURFACE_V0.md` still documents defaults `symbol=BTC&#47;EUR`, `source=kraken` (public network OHLCV). Runbook §1 requires `FUTURES_ONLY`, `BITCOIN_DIRECTION_ALLOWED=false`, `NO_REQUEST_TIME_NETWORK_ACCESS=true`. |
| Status | **DOCUMENTED as Discovery/Default Gap** in Runbook §1 (`CURRENT_REPO_OR_DOCS_DEFAULT_MAY_INCLUDE_KRAKEN_OR_BTC=true`; must not claim current default as already compliant). Runtime defaults unchanged; fix requires separate bounded slice. |

### R5 — Narrow Discovery wording for visual-operator package path

| Field | Value |
|-------|-------|
| Severity | LOW |
| Finding | Discovery lists surface builders as symbols on `market_surface.py`; actual display adapters live under `src/webui/market_visual_operator_surface_v1/`. `market_surface.py` imports and orchestrates them. |
| Recommended patch | Extend §0A baseline with package path + orchestration note (reuse, no second chain). |

### R6 — Browser E2E baseline still missing as product-grade harness

| Field | Value |
|-------|-------|
| Severity | MED |
| Finding | `MARKET_BROWSER_E2E_BASELINE=MISSING` remains accurate for Playwright/real-browser harness. Many focused `tests&#47;webui&#47;test_market_*` structure/visual contracts exist, but not a durable browser E2E baseline matching §8.7/§10. |
| Recommended patch | Keep `MISSING`; bind Phase **10** / Phase **1A** evidence to existing structure contracts as interim, and name the future E2E owner explicitly when created. |

### R7 — PR / main relative status note

| Field | Value |
|-------|-------|
| Severity | MED (process) |
| Finding | Discovery baseline HEAD is the feature tip; `origin&#47;main` is **one commit behind** this branch (`e20b788…`). PR 5244 is **OPEN**, not merged. |
| Recommended patch | Before each mutation slice: re-record `ORIGIN_MAIN`, PR state, and whether Discovery baseline still equals feature HEAD. Do not treat Discovery SHAs as merge authority. |

### R8 — Primary CSS owner remains fragmented (D10)

| Field | Value |
|-------|-------|
| Severity | MED (matches known defect) |
| Finding | `PRIMARY_CSS_OWNER=market_v0.html+base.html` is still true; Tailwind CDN in `base.html`; no single token owner file yet. |
| Recommended patch | No Discovery correction needed; track closure under Phase **1B** only. |

### R9 — Detail Chart.js CDN still primary on legacy/detail panels

| Field | Value |
|-------|-------|
| Severity | MED (matches D9) |
| Finding | CDN `chart.js@4.4.1` remains in Double-Play / legacy panels; vendor fallback exists. Primary operator chart is SSR SVG (aligned). |
| Recommended patch | Keep Discovery accurate; Phase **1B** must vendorize/remove CDN for target allowlist `SELF_ONLY`. |

### R10 — Phase -1 discovery artifacts not yet materialized in-repo

| Field | Value |
|-------|-------|
| Severity | MED |
| Finding | Runbook Phase -1 lists `implementation_discovery_report.md`, `dashboard_requirement_traceability_matrix.json`, etc. This bootstrap does **not** create those (planning + patch list only). |
| Recommended patch | Either (a) mark Phase -1 artifacts as satisfied by PR 5244 discovery evidence + this bootstrap companion set, or (b) produce them in a dedicated Phase -1/0 docs PR before UI mutation. Prefer (b) if strict Phase -1 exit criteria are enforced. |

---

## Explicit non-actions

```text
NO_SILENT_RUNBOOK_EDIT=true
NO_RUNTIME_CODE_CHANGE=true
NO_TRADING_SEMANTICS_CHANGE=true
NO_MARKET_SURFACE_SSOT_REWRITE_IN_BOOTSTRAP=true
NO_DEFAULT_QUERY_MUTATION=true
```

## Next allowed action after review

1. Operator accepts or rejects each `R*` item.  
2. Accepted items land as a **docs-only** Runbook patch PR (or Phase -1 artifact PR).  
3. Only then start `PHASE_1A_LAYOUT_AND_HEADER` under a separate GO.

