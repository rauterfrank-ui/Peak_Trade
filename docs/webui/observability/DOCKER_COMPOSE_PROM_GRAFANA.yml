version: "3.9"

services:
  prometheus:
    image: prom/prometheus:latest
    volumes:
      - ./PROMETHEUS_SCRAPE_EXAMPLE.yml:/etc/prometheus/prometheus.yml:ro
    ports:
      - "9090:9090"

  grafana:
    image: grafana/grafana:latest
    ports:
      - "3000:3000"
