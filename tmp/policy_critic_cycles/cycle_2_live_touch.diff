diff --git a/src/live/session_registry.py b/src/live/session_registry.py
index 3333333..4444444 100644
--- a/src/live/session_registry.py
+++ b/src/live/session_registry.py
@@ -10,6 +10,10 @@ class LiveSessionRegistry:
     def register(self, session):
+        # NOTE(governance): touched in G3.5 synthetic cycle to validate critical-path gating.
+        # No behavioral change expected.
+        # Keep hard-gates sovereign.
         self._sessions[session.session_id] = session
